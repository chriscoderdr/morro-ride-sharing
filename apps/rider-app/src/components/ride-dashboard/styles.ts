import { StyleSheet, ViewStyle } from 'react-native';

interface IRideDashboardStyles {
  errorsContainer: ViewStyle;
}

export const styles = StyleSheet.create({
  errorsContainer: {
    marginVertical: 10
  }
});
